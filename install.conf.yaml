- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- brewfile:
    - brew/Brewfile

- link:
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.config/fish: fish

- shell:
    - [sh scripts/macos.sh, Running setup script]
